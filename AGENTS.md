# StudyQuestionBankAssistant 规则

## 范围

此目录是本地学习型题库助手项目的专用工作区。

项目范围包括：

- 构建可维护的题库检索服务
- 支持本地或自托管的大模型推理
- 支持手动审核候选答案和解析
- 保留来源属性、日志和可测试性

项目目标不包括：

- 自动提交答案至课程、考试或家庭作业平台
- 绕过平台限制或防滥用控制
- 共享私有答案 API、泄露的密钥或抓取的受保护数据集

## 必需参考

在进行实现更改之前，请阅读并对齐：

- 已安装的 Codex 技能 `general-development-standards/SKILL.md`
- OCS 接口文档：`https://docs.ocsjs.com/docs/other/api/`

这里只将 OCS 文档用作接口参考。项目本身应视为学习助手和检索服务，而不是自动化提交脚本。

## 仓库约定

- 所有新文件都放在本项目目录内，并遵循现有目录边界
- 优先做小范围、可审查、可回滚的改动，不顺手扩散到无关模块
- 新增共享逻辑前先复用现有服务、仓储、配置和文档位置
- 不要把 Token、API Key、私有数据集、个人账号信息写入可提交文件
- 文档、脚本、配置若成为团队共用入口，必须同步更新其附近说明

## 开发环境

- Python 依赖和运行默认使用 Conda
- 建议环境名称前缀为 `ai-`
- 当前推荐环境名称为 `ai-study-qb`
- 本地启动入口以 `scripts/run.ps1`、`scripts/run.sh` 为准（可附加 `-Dev` / `--dev` 开启开发模式热重载）

## 发布与同步

- 任何“同步到服务器”“打包上传”“部署镜像”“在服务器执行 `docker compose up -d --build`”一类动作，都视为一次发布动作
- 每次发布动作开始前，必须先更新 [pyproject.toml](pyproject.toml) 中的 `project.version`
- 如果本次变更会同步到服务器，但 `pyproject.toml` 版本号没有变化，应先补版本号，再继续后续同步
- 版本号更新应与本次发布内容保持对应，不允许服务器上出现“代码已更新但版本号未变化”的状态
- 每次发布提交后必须创建对应的 Git tag，格式为 `vX.Y.Z`，且 tag 版本必须与 `pyproject.toml` 中的 `project.version` 一致
- 若本次只是本地调试、测试或未发生服务器同步，则不要为了凑流程随意改版本号

### 版本号规则

项目在 `1.0.0` 前使用 `0.MINOR.PATCH` 三段式版本号，版本升级必须按发布内容选择，不得只递增末位。

- `PATCH`：仅包含向后兼容的缺陷修复、性能修复或不新增用户可用能力的维护改动时，递增末位，例如 `0.1.35 -> 0.1.36`。
- `MINOR`：新增完整的用户可用功能、管理能力、独立 API 资源或重要的向后兼容能力时，递增中间位并将末位归零，例如 `0.1.36 -> 0.2.0`。
- `MAJOR`：项目进入稳定 `1.0.0` 后，存在破坏性 API、数据或部署兼容性变更时递增主版本。`1.0.0` 前如出现需要迁移的不兼容变更，至少递增 `MINOR`，并在发布说明中写明迁移要求。
- 可在功能完成、等待发布的本地工作区预先更新 `pyproject.toml` 版本；此时不得创建对应 Git tag、推送或部署。只有完成验证并提交发布改动后，才能创建匹配 tag 并同步服务器。

推荐发布顺序：

1. 完成功能改动并自检
2. 更新 `pyproject.toml` 版本号
3. 运行最小必要验证
4. 提交本次发布改动并创建对应版本 tag
5. 再执行服务器同步、镜像构建或部署命令

## 文档布局

- `README.md`：项目概述、启动、验证、发布入口与文档导航。
- `docs/README.md`：长期维护文档的唯一导航入口，按架构、服务、设置和流程分组。
- `src/study_qb_assistant/README.md`：后端业务边界和目录约定。
- `scripts/README.md`：运行与本地维护脚本的入口说明。
- `tests/README.md`：测试分层、运行命令和测试数据边界。
- 一次性排查报告、截图和临时实验记录放在 `.local/` 或发布说明中，不作为根目录长期文档提交。

## 测试布局

- 所有自动化测试、回归测试和测试夹具必须放在 `tests/`。
- 需要人工执行、会调用外部模型或会直接修改本地测试数据的验证脚本放在 `tests/manual/`，并明确标注不可用于生产数据。
- `scripts/` 只保留项目运行、部署和可复用维护入口；不得放置测试模型初始化、一次性验证或探索脚本。
- 不在仓库根目录、`src/` 或部署目录放置 `test_*.py`、临时测试 SQL 或测试输出文件。

## 交付检查

在交付任何可提交改动前，请至少确认：

- 未提交任何密钥、密码、私有令牌或个人敏感信息
- 未把用户机器上的绝对路径写进项目主链路
- 未在没有配置入口的情况下硬编码持久化路径
- 代码改动已经完成最小必要验证
- 公共接口、部署步骤或操作流程变化时，文档已同步更新
- 如果本次要同步到服务器，`pyproject.toml` 版本号已先更新，并已创建匹配的 `vX.Y.Z` tag
